import {notFound} from "next/navigation"; import Link from "next/link"; import {researchArticles} from "@/lib/data/research";
export default async function ResearchDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const a=researchArticles.find(x=>x.slug===slug); if(!a)notFound(); return <article className="mx-auto max-w-3xl space-y-8 p-6 py-12 sm:p-8"><Link href="/research" className="text-sm font-medium text-emerald-700">← Research library</Link><header><p className="text-xs font-medium uppercase tracking-wide text-emerald-700">{a.category} · {a.minutes} min read</p><h1 className="mt-3 text-3xl font-semibold">{a.title}</h1><p className="mt-3 text-lg text-neutral-600">{a.summary}</p></header><div className="rounded-lg border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900">Educational material only. MIRROR does not provide personalized investment advice or execution.</div><p className="text-base leading-8 text-neutral-700">{a.content}</p></article>}
