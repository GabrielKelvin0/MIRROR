import {requireRole} from "@/lib/auth/session"; import {adminRepository} from "@/lib/db"; import RoleForm from "./role-form";
export default async function Users(){await requireRole("ADMIN");const users=await adminRepository.listUsers();return <div className="mx-auto max-w-6xl space-y-6 p-6 sm:p-8"><h1 className="text-3xl font-semibold">Users</h1><p className="text-neutral-600">Review local accounts and manage platform roles.</p>{users.length===0?<p>No users yet.</p>:<div className="overflow-x-auto rounded-lg border bg-white"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-4">User</th><th className="p-4">Role</th><th className="p-4">Strategies</th><th className="p-4">Joined</th><th className="p-4">Change role</th></tr></thead><tbody>{users.map(u=><tr key={u.id} className="border-b last:border-0"><td className="p-4"><div>{[u.firstName,u.lastName].filter(Boolean).join(" ")||"Unnamed user"}</div><div className="text-xs text-neutral-500">{u.email}</div></td><td className="p-4">{u.role}</td><td className="p-4">{u._count.strategies}</td><td className="p-4">{u.createdAt.toLocaleDateString()}</td><td className="p-4"><RoleForm userId={u.id} role={u.role}/></td></tr>)}</tbody></table></div>}</div>}