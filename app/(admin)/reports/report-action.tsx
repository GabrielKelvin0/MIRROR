"use client"; import {resolveReport} from "../actions"; export default function ReportAction({id,status}:{id:string;status:"OPEN"|"INVESTIGATING"|"RESOLVED"|"DISMISSED"}){if(status==="RESOLVED"||status==="DISMISSED")return <span className="text-xs text-neutral-500">{status}</span>;return <div className="flex gap-2"><button onClick={async()=>{await resolveReport(id,"RESOLVED");location.reload()}} className="rounded bg-emerald-700 px-3 py-1 text-xs text-white">Resolve</button><button onClick={async()=>{await resolveReport(id,"DISMISSED");location.reload()}} className="rounded border px-3 py-1 text-xs">Dismiss</button></div>}