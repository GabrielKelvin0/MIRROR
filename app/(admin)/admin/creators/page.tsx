import {requireRole} from "@/lib/auth/session"; import {adminRepository} from "@/lib/db"; export default async function Creators(){await requireRole("ADMIN");const creators=await adminRepository.listCreators();return <div className="mx-auto max-w-6xl space-y-6 p-6 sm:p-8"><h1 className="text-3xl font-semibold">Creators</h1><p className="text-neutral-600">Creator accounts and their strategy footprint.</p>{creators.length===0?<p>No creator accounts yet.</p>:<div className="grid gap-4 md:grid-cols-2">{creators.map(c=><div key={c.id} className="rounded-lg border bg-white p-5"><h2 className="font-semibold">{[c.firstName,c.lastName].filter(Boolean).join(" ")||"Unnamed creator"}</h2><p className="text-sm text-neutral-500">{c.email}</p><p className="mt-3 text-sm">{c._count.strategies} strategies · {c.creatorProfile?.followerCount??0} followers</p><p className="mt-1 text-xs text-neutral-500">Joined {c.createdAt.toLocaleDateString()} · {c.creatorProfile?.isVerified?"Verified":"Unverified"}</p></div>)}</div>}</div>}